import { DetalheProdDirective } from './detalhe-prod.directive';

describe('DetalheProdDirective', () => {
  it('should create an instance', () => {
    const directive = new DetalheProdDirective();
    expect(directive).toBeTruthy();
  });
});
