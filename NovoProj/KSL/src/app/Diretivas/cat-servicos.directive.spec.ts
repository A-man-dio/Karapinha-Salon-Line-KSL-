import { CatServicosDirective } from './cat-servicos.directive';

describe('CatServicosDirective', () => {
  it('should create an instance', () => {
    const directive = new CatServicosDirective();
    expect(directive).toBeTruthy();
  });
});
